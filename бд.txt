CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    login VARCHAR(50) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(150) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    role ENUM('user','admin') DEFAULT 'user',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


CREATE TABLE applications (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    course_name VARCHAR(150) NOT NULL,
    start_date DATE NOT NULL,
    payment_method ENUM('Наличными','Перевод по номеру телефона') NOT NULL,
    status ENUM('Новая','Идет обучение','Обучение завершено') DEFAULT 'Новая',
    review TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_app_user
        FOREIGN KEY (user_id) REFERENCES users(id)
        ON DELETE CASCADE
);

CREATE INDEX idx_users_login ON users(login);
CREATE INDEX idx_app_user_id ON applications(user_id);
CREATE INDEX idx_app_status ON applications(status);

INSERT INTO users (login, password_hash, full_name, phone, email, role)
VALUES (
    'Admin',
    '$2b$10$examplehashreplaceit', -- заменить на реальный bcrypt/argon2 хэш
    'Администратор системы',
    '8(000)000-00-00',
    'admin@portal.ru',
    'admin'
);



ALTER TABLE users
ADD CONSTRAINT chk_login
CHECK (login REGEXP '^[A-Za-z0-9]{6,}$'),
ADD CONSTRAINT chk_phone
CHECK (phone REGEXP '^8\\([0-9]{3}\\)[0-9]{3}-[0-9]{2}-[0-9]{2}$'),
ADD CONSTRAINT chk_fullname
CHECK (full_name REGEXP '^[А-Яа-яЁё ]+$');